import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'redrot-disease-recognizer',
  webDir: 'www'
};

export default config;
